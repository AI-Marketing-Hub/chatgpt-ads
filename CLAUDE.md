# Claude entrypoint

Read `AGENTS.md`, then `SKILL.md` and `runtime/claude.md`. Skills are shared; native Claude tool availability and acceptance are tracked separately. No automatic installation or provider calls.
